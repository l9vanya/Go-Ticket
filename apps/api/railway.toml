[build]
buildCommand = "npm install && npx prisma generate --schema=apps/api/prisma/schema.prisma && cd apps/api && npx tsc"

[deploy]
startCommand = "node apps/api/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
